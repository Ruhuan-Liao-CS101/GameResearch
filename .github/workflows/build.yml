name: Build Game Executables

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # Allows manual triggering

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pygame pyinstaller
          # Add any other dependencies your project needs

      - name: Build with PyInstaller
        shell: cmd
        run: >
          pyinstaller --name TetrisGame --windowed --onefile
          --add-data "TetrisGame/font/font.ttf;TetrisGame/font/"
          --add-data "TetrisGame/img/background2.webp;TetrisGame/img/"
          --add-data "TetrisGame/img/img1.webp;TetrisGame/img/"
          --add-data "TetrisGame/img/photo1.jpeg;TetrisGame/img/"
          --add-data "TetrisGame/main.py;TetrisGame/"
          --add-data "TetrisGame/tetris_game.py;TetrisGame/"
          --add-data "scenes/avatars;scenes/avatars"
          --add-data "scenes/backgrounds;scenes/backgrounds"
          --add-data "scenes/avatars_data.json;scenes"
          main.py

      - name: Upload Windows Build
        uses: actions/upload-artifact@v4
        with:
          name: TetrisGame-Windows
          path: dist/TetrisGame.exe
